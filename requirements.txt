# 웹 프레임워크
flask==2.3.3
gunicorn==21.2.0
flask-wtf==1.1.1

# 이미지 처리 및 AI (Render 최적화)
deepface==0.0.79
opencv-python-headless==4.8.0.76
tensorflow-cpu==2.13.0  # CPU 버전으로 변경 (Render 최적화)

# 데이터 처리 및 시각화
pandas==2.0.3
matplotlib==3.7.2
numpy==1.24.3

# 보안 및 파일 처리
python-magic==0.4.27
python-magic-bin==0.4.14  # Windows 호환성

# 추가 의존성
pillow==10.0.0
protobuf==3.20.3
werkzeug==2.3.7
sqlite3  # 기본 포함