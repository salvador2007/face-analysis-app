services:
  - type: web
    name: deepface-analyzer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
      - key: PORT
        sync: false